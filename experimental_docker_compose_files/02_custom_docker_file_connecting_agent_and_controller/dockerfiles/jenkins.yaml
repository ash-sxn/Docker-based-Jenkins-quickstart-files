jenkins:
  numExecutors: 0
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "admin"
  authorizationStrategy: loggedInUsersCanDoAnything
  nodes:
    - permanent:
        labelString: "docker jenkins agent"
        launcher:
          ssh:
            credentialsId: "jenkins-ssh-agent-private-key"
            host: "desktop-jenkins_agent-1"
            port: 22
            sshHostKeyVerificationStrategy: "nonVerifyingKeyVerificationStrategy"
        name: "docker-ssh-jenkins-agent"
        nodeDescription: "ssh jenkins docker agent "
        remoteFS: "/home/jenkins/agent"
        retentionStrategy: "always"
credentials:
  system:
    domainCredentials:
      - credentials:
          - basicSSHUserPrivateKey:
              id: "jenkins-ssh-agent-private-key"
              description: "private ssh key for jenkins agent "
              privateKeySource:
                directEntry:
                  privateKey: "${SSH_AGENT_KEY}"
              scope: SYSTEM
              username: "jenkins"
unclassified:
  location:
    url: "https://8080-machine_name=ashsxn-gsoc2023dockerba-od66he6n77h.ws-us100.gitpod.io/"
